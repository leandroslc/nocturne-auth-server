import { ElementHelperProvider } from '@/src/base/providers/types';

const provider: ElementHelperProvider = {
  autofocusAttribute: '[autofocus]',
  inputPasswordElement: 'is-password',
};

export default provider;
