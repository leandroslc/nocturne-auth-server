import { SidenavProvider } from '@/src/base/providers/types';

const provider: SidenavProvider = {
  expandedClass: 'is-expanded',
  backdropClass: 'sidenav-backdrop',
  toggleAttribute: 'sidenav-toggle',
};

export default provider;
